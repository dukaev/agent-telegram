# agent-telegram

Telegram automation CLI for AI agents. Fast Go binary with MTProto API.

By Aslan Dukaev [X](https://x.com/dukaev) ·[Telegram](https://t.me/dukaev)

![Screenshot](docs/screen.png)

## Installation

### Package Managers

```bash
bun add -g agent-telegram
npm install -g agent-telegram
```

### From Source

```bash
git clone https://github.com/dukaev/agent-telegram
cd agent-telegram
go build -o agent-telegram .
```

## Quick Start

```bash
AGENT_TELEGRAM_PHONE=+123 agent-telegram auth web  # Local browser login
AGENT_TELEGRAM_PHONE=+123 agent-telegram auth web --qr # QR code login
agent-telegram auth web --mock           # Try the web login UI with mock data
agent-telegram server ensure              # Start IPC server explicitly
agent-telegram my-info                    # Get your profile
agent-telegram chat list                  # List all chats
agent-telegram chat open @username        # View messages from chat
agent-telegram send @user "Hello!"        # Send message
agent-telegram stop                       # Stop server
```

## Commands

<!-- BEGIN GENERATED:commands -->
<!-- This section is generated by `agent-telegram docs generate`; do not edit by hand. -->

| Command | Description | Method | Safety |
|---------|-------------|--------|--------|
| `agent-telegram audit` | Read redacted action audit events | - | - |
| `agent-telegram docs` | Generate and check documentation from code | - | - |
| `agent-telegram docs check` | Check generated documentation blocks | - | - |
| `agent-telegram docs generate` | Update generated documentation blocks | - | - |
| `agent-telegram logs` | Read agent-telegram logs | - | - |
| `agent-telegram manifest` | Output machine-readable operation manifest | - | - |
| `agent-telegram run` | Inspect one agent run | - | - |
| `agent-telegram run inspect` | Inspect audit and logs for a run ID | - | - |
| `agent-telegram serve` | Start IPC server with Telegram (Unix socket) | - | - |
| `agent-telegram serve-api` | Start HTTP REST API server with Telegram | - | - |
| `agent-telegram server` | Manage the IPC server lifecycle | - | - |
| `agent-telegram server ensure` | Start IPC server if needed and wait until ready | - | - |
| `agent-telegram server wait-ready` | Wait until IPC server is ready | - | - |
| `agent-telegram session` | Manage Telegram session | - | - |
| `agent-telegram session export` | Export session as base64 for TELEGRAM_SESSION env | - | - |
| `agent-telegram status` | Check if the IPC server is running | - | - |
| `agent-telegram stop` | Stop the IPC server | - | - |
| `agent-telegram trace` | Inspect one traced operation | - | - |
| `agent-telegram trace inspect` | Inspect audit and logs for a trace ID | - | - |
| `agent-telegram auth` | Headless Telegram authentication | - | - |
| `agent-telegram auth begin` | Send Telegram login code | - | - |
| `agent-telegram auth password` | Verify Telegram 2FA password from stdin | - | - |
| `agent-telegram auth status` | Show local authentication status | - | - |
| `agent-telegram auth verify` | Verify Telegram login code from stdin | - | - |
| `agent-telegram auth web` | Login through a local browser page | - | - |
| `agent-telegram logout` | Logout and clear Telegram session | - | - |
| `agent-telegram my-info` | Get your profile information | `get_me` | `read` |
| `agent-telegram bot` | High-level bot flow commands | - | - |
| `agent-telegram bot press` | Press an inline button in a bot flow | - | - |
| `agent-telegram bot step` | Send optional text and return bot state | - | - |
| `agent-telegram msg` | Message management commands | - | - |
| `agent-telegram msg clear` | Clear messages or history | `clear_messages` | `destructive` |
| `agent-telegram msg delete` | Delete Telegram message(s) | `delete_message` | `destructive` |
| `agent-telegram msg forward` | Forward a Telegram message to another user or chat | `forward_message` | `write` |
| `agent-telegram msg get` | Get a single message by ID | `get_message` | `read` |
| `agent-telegram msg inspect-buttons` | Inspect inline buttons in a message | `inspect_inline_buttons` | `read` |
| `agent-telegram msg inspect-keyboard` | Inspect reply keyboard buttons from a chat | `inspect_reply_keyboard` | `read` |
| `agent-telegram msg list` | Get messages from a chat | `get_messages` | `read` |
| `agent-telegram msg pin` | Pin or unpin a Telegram message | `pin_message` | `write` |
| `agent-telegram msg press-button` | Press an inline button in a message | `press_inline_button` | `write` |
| `agent-telegram msg press-keyboard` | Press a reply keyboard button | - | - |
| `agent-telegram msg reaction` | Add a reaction to a Telegram message | `add_reaction` | `write` |
| `agent-telegram msg read` | Mark messages as read | `read_messages` | `write` |
| `agent-telegram msg replies` | Get replies (comments) to a channel post | `get_replies` | `read` |
| `agent-telegram msg reply-comment` | Reply to a comment in a channel post discussion | `reply_to_comment` | `write` |
| `agent-telegram msg scheduled` | List scheduled messages | `get_scheduled_messages` | `read` |
| `agent-telegram msg typing` | Send typing indicator | `set_typing` | `write` |
| `agent-telegram msg wait` | Wait for the next incoming message | - | - |
| `agent-telegram send` | Send a message or media to a Telegram peer | `send_message` | `write` |
| `agent-telegram send contact` | Send a contact | `send_contact` | `write` |
| `agent-telegram send dice` | Send a dice (random value) | `send_dice` | `write` |
| `agent-telegram send location` | Send a location | `send_location` | `write` |
| `agent-telegram send photo` | Send a photo | `send_photo` | `write` |
| `agent-telegram send poll` | Send a poll | `send_poll` | `write` |
| `agent-telegram send sticker` | Send a sticker | `send_sticker` | `write` |
| `agent-telegram send text` | Send a text message | `send_message` | `write` |
| `agent-telegram send update` | Edit a previously sent message | `update_message` | `write` |
| `agent-telegram send video` | Send a video | `send_video` | `write` |
| `agent-telegram send voice` | Send a voice message | `send_voice` | `write` |
| `agent-telegram balance` | Show stars and TON balance | `get_balance` | `read` |
| `agent-telegram chat` | Manage Telegram chats and channels | - | - |
| `agent-telegram chat admins` | List admins in a chat or channel | `get_admins` | `read` |
| `agent-telegram chat archive` | Archive or unarchive a Telegram chat | `archive` | `write` |
| `agent-telegram chat banned` | List banned users in a channel | `get_banned` | `read` |
| `agent-telegram chat create-channel` | Create a new channel or supergroup | `create_channel` | `write` |
| `agent-telegram chat create-group` | Create a new group chat | `create_group` | `write` |
| `agent-telegram chat delete-photo` | Delete the photo from a chat or channel | `delete_photo` | `destructive` |
| `agent-telegram chat demote-admin` | Demote an admin to regular user | `demote_admin` | `write` |
| `agent-telegram chat edit-title` | Edit the title of a chat or channel | `edit_title` | `write` |
| `agent-telegram chat info` | Get information about a chat or channel | `get_chats` | `read` |
| `agent-telegram chat invite` | Invite users to a chat or channel | `invite` | `write` |
| `agent-telegram chat invite-link` | Get or create an invite link for a chat or channel | `get_invite_link` | `read` |
| `agent-telegram chat join` | Join a chat using an invite link | `join_chat` | `write` |
| `agent-telegram chat keyboard` | Inspect reply keyboard in a chat | `inspect_reply_keyboard` | `read` |
| `agent-telegram chat leave` | Leave a chat or channel | `leave` | `destructive` |
| `agent-telegram chat list` | List Telegram chats | `get_chats` | `read` |
| `agent-telegram chat mute` | Mute or unmute a Telegram chat | `mute` | `write` |
| `agent-telegram chat open` | Open and view messages from a Telegram user/chat | `get_messages` | `read` |
| `agent-telegram chat participants` | List participants in a chat or channel | `get_participants` | `read` |
| `agent-telegram chat permissions` | Set default chat permissions | `set_chat_permissions` | `write` |
| `agent-telegram chat pin` | Pin or unpin a chat in the dialog list | `pin_chat` | `write` |
| `agent-telegram chat promote-admin` | Promote a user to admin | `promote_admin` | `write` |
| `agent-telegram chat set-photo` | Set the photo for a chat or channel | `set_photo` | `write` |
| `agent-telegram chat slow-mode` | Set slow mode for a channel | `set_slow_mode` | `write` |
| `agent-telegram chat subscribe` | Subscribe to a public channel | `subscribe_channel` | `write` |
| `agent-telegram chat topics` | List forum topics in a channel | `get_topics` | `read` |
| `agent-telegram chats` | List Telegram chats | `get_chats` | `read` |
| `agent-telegram contact` | Manage Telegram contacts | - | - |
| `agent-telegram contact add` | Add a new contact to your Telegram account | `add_contact` | `write` |
| `agent-telegram contact delete` | Delete a contact from your Telegram account | `delete_contact` | `destructive` |
| `agent-telegram contact list` | List contacts from your Telegram account | `get_contacts` | `read` |
| `agent-telegram game` | Play games in Telegram chats | - | - |
| `agent-telegram game dice` | Roll a dice in a Telegram chat | - | - |
| `agent-telegram gift` | Manage Telegram star gifts | - | - |
| `agent-telegram gift accept` | Accept an incoming gift offer | `accept_gift_offer` | `paid` |
| `agent-telegram gift attrs` | List available attributes for a gift type | `get_gift_attrs` | `read` |
| `agent-telegram gift convert` | Convert a star gift to stars | `convert_star_gift` | `destructive` |
| `agent-telegram gift decline` | Decline an incoming gift offer | `decline_gift_offer` | `destructive` |
| `agent-telegram gift info` | Show detailed info and value analytics for a unique gift | `get_gift_info` | `read` |
| `agent-telegram gift list` | List gift catalog, user's saved gifts, or marketplace | `get_saved_gifts` | `read` |
| `agent-telegram gift offer` | Make an offer to buy someone's gift | `offer_gift` | `paid` |
| `agent-telegram gift price` | Set resale price on a star gift | `update_gift_price` | `paid` |
| `agent-telegram gift send` | Send, transfer, or buy a gift | `send_star_gift` | `paid` |
| `agent-telegram open` | Open a chat, join via invite link, or view a gift | - | - |
| `agent-telegram search` | Search Telegram content | - | - |
| `agent-telegram search global` | Search public chats, channels, and bots globally | `search_global` | `read` |
| `agent-telegram search in-chat` | Search for messages within a specific chat | `search_in_chat` | `read` |
| `agent-telegram updates` | Get Telegram updates (pops from store) | `get_updates` | `read` |
| `agent-telegram user` | Manage user-related operations | - | - |
| `agent-telegram user ban` | Block or unblock a Telegram peer | `block` | `destructive` |
| `agent-telegram user info` | Get information about a Telegram user | `get_user_info` | `read` |
| `agent-telegram user mute` | Mute or unmute a Telegram chat | `mute` | `write` |
| `agent-telegram completion` | Generate the autocompletion script for the specified shell | - | - |
| `agent-telegram completion bash` | Generate the autocompletion script for bash | - | - |
| `agent-telegram completion fish` | Generate the autocompletion script for fish | - | - |
| `agent-telegram completion powershell` | Generate the autocompletion script for powershell | - | - |
| `agent-telegram completion zsh` | Generate the autocompletion script for zsh | - | - |
| `agent-telegram folders` | Manage chat folders | - | - |
| `agent-telegram folders create` | Create a new chat folder | `create_folder` | `write` |
| `agent-telegram folders delete` | Delete a chat folder | `delete_folder` | `destructive` |
| `agent-telegram folders list` | List all chat folders | `get_folders` | `read` |
| `agent-telegram llms-txt` | Generate full CLI documentation for LLMs | - | - |
| `agent-telegram privacy` | Manage privacy settings | - | - |
| `agent-telegram privacy get` | Get privacy setting | `get_privacy` | `read` |
| `agent-telegram privacy set` | Set privacy setting | `set_privacy` | `write` |
| `agent-telegram skills` | List and install bundled agent skills | - | - |
| `agent-telegram skills install` | Install a bundled agent skill | - | - |
| `agent-telegram skills list` | List bundled agent skills | - | - |
| `agent-telegram skills path` | Show the default agent skill install directory | - | - |
<!-- END GENERATED:commands -->

## Global Options

<!-- BEGIN GENERATED:global-options -->
<!-- This section is generated by `agent-telegram docs generate`; do not edit by hand. -->

| Option | Description |
|--------|-------------|
| `--agent` | Enable agent-friendly compact JSON, receipts, run IDs, and structured errors |
| `--dry-run` | Preview action without executing |
| `--filter <strings>` | Filter results (e.g., 'stars>1000', 'type=channel') |
| `--include <strings>` | Include output fields (comma-separated, supports dot paths) |
| `--max-items <int>` | Maximum array items in JSON output (0 uses verbosity default) |
| `--max-text-chars <int>` | Maximum text field characters in JSON output (0 uses verbosity default) |
| `--omit <strings>` | Omit output fields (comma-separated, supports dot paths) |
| `--output <string>` | Output format: json or ids |
| `-q, --quiet` | Suppress status messages (data still goes to stdout) |
| `--receipt` | Wrap JSON output with trace/action receipt metadata |
| `--run-id <string>` | Agent run ID for correlating multiple commands |
| `--schema` | Output operation schema without executing |
| `-s, --socket <string>` | Path to Unix socket (default: /tmp/agent-telegram.sock) |
| `--summary` | Output a compact result summary |
| `--verbosity <string>` | Output detail: minimal, compact, full, raw (default: full) |
<!-- END GENERATED:global-options -->

## Environment Variables

| Variable | Description |
|----------|-------------|
| `TELEGRAM_APP_ID` | Telegram API App ID (optional, has default) |
| `TELEGRAM_APP_HASH` | Telegram API App Hash (optional, has default) |
| `AGENT_TELEGRAM_PHONE` | Phone number for auth (optional; safer than passing phone in argv) |
| `AGENT_TELEGRAM_RPC_TIMEOUT` | RPC handler timeout, e.g. `45s` or `2m` |
| `AGENT_TELEGRAM_API_SECRET` | Bearer token for `serve-api` |
| `AGENT_TELEGRAM_RUN_ID` | Optional run ID shared across agent commands |

Default API credentials are built-in, so you can start using agent-telegram immediately. To use your own credentials, get them at https://my.telegram.org and set via environment variables or `.env` file.

## Pipe-Friendly Patterns

All commands output JSON to stdout by default. Status/logs go to stderr.

```bash
# Get message IDs for piping
agent-telegram msg list @user --output ids | head -5

# Filter and select fields
agent-telegram chat list --include peer,title,type --filter type=channel

# Continuous monitoring with jq
agent-telegram updates --follow --type new_message | jq '{from: .data.message.from_name, text: .data.message.text}'

# Quiet mode (suppress stderr, data only)
agent-telegram msg list @user -q | jq '.messages[].text'

# Compose commands
agent-telegram search global "bot" --output ids | head -3
```

## Usage with AI Agents

### Just ask the agent

The simplest approach - just tell your agent to use it:

```
Use agent-telegram to send a message to @username. Run agent-telegram --help to see available commands.
```

```markdown
## Telegram Automation

Use `agent-telegram` for Telegram automation. Run `agent-telegram --help` for all commands.

Core workflow:
1. `agent-telegram server ensure` - Start or verify the server
2. `agent-telegram status` - Verify connection
3. `agent-telegram chat list` - List available chats
4. `agent-telegram msg list @user` - Read messages
5. `agent-telegram send @user "message"` - Send message
6. `agent-telegram updates --follow` - Monitor for new messages
```

## IPC Protocol (JSON-RPC)

All commands communicate via JSON-RPC over Unix socket at `/tmp/agent-telegram.sock`:

```bash
# Direct IPC call
echo '{"method":"send_message","params":{"peer":"@user","message":"Hi"}}' | nc -U /tmp/agent-telegram.sock
```

### Available Methods (77+)

**Messages:** `send_message`, `send_reply`, `update_message`, `delete_message`, `forward_message`, `get_messages`, `clear_messages`, `clear_history`, `read_messages`, `set_typing`, `get_scheduled_messages`

**Media:** `send_photo`, `send_video`, `send_file`, `send_voice`, `send_video_note`, `send_sticker`, `send_gif`, `send_location`, `send_contact`, `send_poll`, `send_dice`

**Reactions:** `add_reaction`, `remove_reaction`, `list_reactions`

**Buttons:** `inspect_inline_buttons`, `press_inline_button`, `inspect_reply_keyboard`

**Pins:** `pin_message`, `unpin_message`, `pin_chat`

**Chats:** `get_chats`, `get_topics`, `create_group`, `create_channel`, `edit_title`, `set_photo`, `delete_photo`, `leave`, `invite`, `join_chat`, `subscribe_channel`, `get_invite_link`

**Members:** `get_participants`, `get_admins`, `get_banned`, `promote_admin`, `demote_admin`

**Settings:** `set_slow_mode`, `set_chat_permissions`

**Folders:** `get_folders`, `create_folder`, `delete_folder`

**Users:** `get_me`, `get_user_info`, `update_profile`, `update_avatar`, `block`, `unblock`

**Contacts:** `get_contacts`, `add_contact`, `delete_contact`

**Privacy:** `get_privacy`, `set_privacy`

**Search:** `search_global`, `search_in_chat`

**Updates:** `get_updates`

**System:** `status`, `shutdown`, `ping`

### Agentic Tooling

Authentication is headless. Prefer `agent-telegram auth web --qr` for QR login
or `AGENT_TELEGRAM_PHONE=... agent-telegram auth web` for a local one-time
browser form; it prints a localhost URL on stderr and final JSON on stdout.
Supplying the phone through the environment avoids exposing it in the process
list. For pure terminal flows, use `auth begin`, pipe the
Telegram code into `auth verify --code-stdin`, and pipe the 2FA password into
`auth password --password-stdin` if needed. Temporary auth state is stored
locally with owner-only permissions and a TTL.

#### Web Auth

`auth web` starts a loopback-only browser flow. The React page is just the UI:
Telegram QR/code login, session export, and IPC reload happen in the local Go
process. The browser does not receive Telegram session bytes. After successful
login, the runtime session is kept in memory; the temporary auth state is
deleted when the flow finishes or expires.

Use mock mode to develop and test the web UI without Telegram:

```bash
# QR flow with mock dialogs and a "Simulate QR scan" button
agent-telegram auth web --mock

# Code + 2FA flow with mock credentials
agent-telegram auth web --mock --qr=false
# code: 22222
# password: mock-password
```

Mock mode returns a fixed dialog list with users, groups, channels, and bots,
does not contact Telegram, does not reload the IPC server, and does not write
the selected policy to the user's real policy file.

The web UI can be hosted remotely only if it talks to a local
`agent-telegram auth web` backend running on the user's machine. In that setup,
the session stays with the client because the local backend owns the Telegram
login. If the auth backend itself runs on a remote server, that server receives
the Telegram session and must be treated as trusted infrastructure. A remote
static frontend also needs explicit loopback API discovery/CORS handling before
it can call the local backend safely.

Use `agent-telegram <command> --schema` for per-command input/output schemas,
safety metadata (`read`, `write`, `destructive`, `paid`), idempotency, retry
hints, and confirmation requirements. Use `agent-telegram manifest` for the
full catalog, or `agent-telegram manifest --openapi` for an OpenAPI document.
Use `agent-telegram skills list` to discover bundled Codex skills and
`agent-telegram skills install agent-telegram` to install the skill locally.

The REST API provides `GET /manifest` and `GET /openapi.json`. RPC calls support
`?dryRun=true`, `?validateOnly=true`, or an envelope body:

```json
{"dryRun": true, "params": {"peer": "@user", "message": "hello"}}
```

REST error responses preserve typed error metadata in `error.data.type`, such as
`PEER_NOT_FOUND`, `FLOOD_WAIT`, `TIMEOUT`, and `VALIDATION`.

For token-efficient reads, use output budgets:

```bash
RUN_ID=run_demo_1
agent-telegram server ensure --agent --run-id "$RUN_ID"
agent-telegram msg list @user --verbosity compact --max-items 5 --max-text-chars 120
agent-telegram msg list @user --summary
agent-telegram msg list @user --omit entities,media
agent-telegram send --to @user "hi" --dry-run --agent --run-id "$RUN_ID"
agent-telegram msg press-button 123 0 --to @bot --wait-reply --agent --run-id "$RUN_ID"
agent-telegram msg wait @bot --after-id 123 --timeout 20s --agent --run-id "$RUN_ID"
agent-telegram bot step @bot --send "/start" --agent --run-id "$RUN_ID"
agent-telegram bot press @bot 123 0 --agent --run-id "$RUN_ID"
```

For analysis and debugging, use redacted audit/log commands:

```bash
agent-telegram audit --last 20
agent-telegram audit --trace-id <traceId>
agent-telegram audit --run-id <runId>
agent-telegram logs --kind cli --last 50
agent-telegram logs --kind server --run-id <runId>
agent-telegram logs --kind server --follow
agent-telegram trace inspect <traceId> --agent
agent-telegram run inspect <runId> --agent
```

Audit/log commands default to `--redaction safe`, which hides free-form message
text, location-like fields, and personal fields in display output. Use
`--redaction redacted` for classic secret redaction with truncated text.
New audit entries are written with safe redaction as well.

## Architecture

agent-telegram uses a client-daemon architecture:

```
┌─────────────┐      IPC       ┌──────────────┐      MTProto
│ CLI Command │ ─────────────> │ IPC Server   │ ──────────────> Telegram
│ (Go binary) │  Unix Socket   │ (background) │   (gotd/td)
└─────────────┘                └──────────────┘
```

- **CLI Commands** - Parse arguments, communicate with daemon via IPC
- **IPC Server** - Background daemon managing Telegram connection
- **Telegram Client** - MTProto client using [gotd/td](https://github.com/gotd/td) library

The daemon is explicit: start or verify it with `agent-telegram server ensure`
before RPC-backed commands, and stop it with `agent-telegram stop` when
finished.

### File Locations

| File | Path |
|------|------|
| Unix socket | `/tmp/agent-telegram.sock` |
| Session | In-memory runtime storage |
| Logs | `~/.agent-telegram/server.log` |
| PID file | `~/.agent-telegram/server.pid` |
| Lock file | `~/.agent-telegram/server.lock` |

Custom `--socket` values get isolated state files with a stable hash suffix
(`server-<hash>.pid`, `server-<hash>.lock`, `server-<hash>.log`) so multiple
instances can run side by side.

## Sessions

Each session maintains its own:
- Telegram connection
- Authentication state
- Message history cache
- Update store

Use `--socket` to run multiple isolated instances:

```bash
# Different sessions
agent-telegram --socket /tmp/agent1.sock serve
agent-telegram --socket /tmp/agent2.sock serve

# Use specific session
agent-telegram --socket /tmp/agent1.sock chat list
```

`serve-api` requires an auth secret by default. For trusted local experiments
only, pass `--unsafe-no-auth` explicitly.

## Development

See [DEVELOPMENT.md](DEVELOPMENT.md) for:
- Architecture overview
- Adding new commands
- Common patterns
- Project structure

## License

MIT
